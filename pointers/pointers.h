
#ifndef ARR_MAX_SIZE
    #define ARR_MAX_SIZE 20
#endif

/**
 * @brief Fill array with random numbers
 *
 * Заполнение массива случайными числами
 *
 * @param arr array to fill
 * @param n указатель на размер массива (number of rows)
 * @param m указатель на размер массива (number of columns)
 */
void fillMass(signed int* arr, int* n, int* m);


/**
 * @brief Print array
 * 
 * Вывод массива
 * 
 * @param arr array to print
 * @param n указатель на размер массива (number of rows)
 * @param m указатель на размер массива (number of columns)
 **/
void printMass(signed int* arr, int* n, int* m);